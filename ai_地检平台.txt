指令序列，保存的时候，数据库需要保存输入控件的输入值，如果不保存，有些属性有公式计算，计算的结果才变成指令，计算的结果在需要还原的时候就不能还原了。界面还原的时候，就用这个保存的值。
如果保存的值在后续输入控件中没了或修改了，对不上的时候，还原的时候，需要符合最新的规则的，比如大小的限定。

→ 已改：序列 JSON 的 ``values`` 存输入控件原值（formula 组帧前）。保存时把当前编辑区一并写入。打开时按当前 component 还原；选项对不上或控件没了用最新默认值，数值超出 min/max 钳到范围内。



单板-相机测试，控制串口性能问题。
ruoyi-fastapi-backend\module_payload\collectors\serial_collector.py的 read_and_parse 函数，
waiting0 = 78680
waiting = 1106578
len(data) = 16384
max_chunks = 128
chunk_size = 16384
我卡断点看到接收数据太多，处理慢，影响了发送数据的处理，造成发送的时候前端都是超时。

进一步分析发现，ruoyi-fastapi-backend\module_payload\collectors\base_collector.py 的 run函数中，这两个函数的执行，self._consume_commands()  和 self.read_and_parse()， read_and_parse的慢速执行，导致run函数卡顿。
我觉得分情况，对于全双工的，收发可以分成两个线程。对于半双工的，收发当前这样处理没问题。

我在配置文件中ruoyi-fastapi-backend\assets\config\cfg_device_connect.json，新增了字段fullDuplex， true是全双工。  以后新增配置，can默认是半双工，网口和串口是半双工。
这个属性，在新建连接的时候，需要传入，然后base_collector的run被调用的时候，首先需要进行全双工和半双工的判断，然后根据不同状态，进行处理。
现在帮我优化代码。

→ 已改：打开连接把 ``fullDuplex`` 写入采集进程（前端传入，缺省按 ``cfg_device_connect.json`` 的 source，再缺省半双工）。``run`` 全双工时 RX 线程跑 ``read_and_parse``，主线程只处理控制/发送/心跳；半双工仍单循环。相机控制/图像/单板串口配置为全双工。


ruoyi-fastapi-backend\module_payload\collectors\serial_collector.py
增加了 MAX_WAITING，read_and_parse中的清理部分代码需要完善代码， 清理本地的缓存数据（组帧缓存），避免旧数据影响新数据处理

→ 已改：RX 积压超过 ``MAX_WAITING`` 时先 ``reset_input_buffer``，再 ``_reset_rx_framing`` 清空 assembler / demux / 插件组帧缓存，避免半截旧帧拼进新数据。



首页/单板/相机测试  页面的   表格中的图像索引，一直是0，
我看到 http://localhost/dev-api/payload/camera/image?port=COM4
返回的"imageNo": 0,  一直是0.

日志区，Recv 01 07 00 00 00 13 24 E5 02 D7 01 2C 01 01 01 14 07 E0 0C E4 03 90 0A ...   这里...的截断太早，在原来基础上增加10个字符的显示。

http://localhost/dev-api/payload/device/io-log?deviceId=source%3Acamera_ctrl&sinceSeq=60363&limit=200
请求返回的日志的，记录之间的间隔太短，下面的日志，记录的hex字段被我裁剪了，不然内容太多。5ms的间隔，太短，改成500ms的。

→ 已改：应答图像序号为 0 时 Redis ``imageNo`` 回退为本次请求索引。传输信息行截断加长 10 字符。Redis 预览 IO 日志同方向最少间隔 500ms，文件落盘仍每包写入。




需要超详细的补全后端的测试用例代码，放tests目录下。

→ 已改：补全 collectors 双工/RX 溢出组帧清理、相机 imageNo、D6 组帧、IO 日志 500ms 节流与 camera service 的后端测试，均放 ``ruoyi-fastapi-backend/tests``。


需要超详细的补全后端的测试用例代码，新增的代码都需要。从hash 16e823021e8603df744ad55f34155a72b4fcc837 开始的git提交都需要。

→ 已改：从 ``16e8230`` 起的 payload 新增逻辑补了后端单测（组帧、遥控公式/values、指令序列、网口 ID 无 ``net:``、fullDuplex、Redis key、CAN 遥测帧、配置加载、会话/配置文件、IO 落盘、网口采集、遥测服务等），均放 ``ruoyi-fastapi-backend/tests``。 CLI/打包/健康检查/相机双工等已有用例未重复。


不需要用真can测试，测试can通讯，使用demo厂商就行，但是不能再一个进程中进行收发

→ 已改：CAN 通讯测 gpcan 演示厂商（vendor=0），收发分两个进程（父进程 SDK 对采集子进程）。同一进程打不开同一条虚拟通道，不用真 CAN / 自发自收。


ruoyi-fastapi-backend\tests\test_can_yc_frame.py的test_hex_to_bytes_odd_nibble 函数，
hex_to_bytes('A B')  == bytes([0xAB]) 是不对的， 应该是， 0x0A 0x0B。
按照我在前端的hex文本的格式化的规则：
空白字符 是 16进制字符的分割
具体示例如下：
a b c -> 0A 0B 0C
ab c -> AB 0C
ab c de f -> AB 0C DE 0F
ab c d -> AB 0C 0D
aabbc  -> AA BB 0C
aabbc d -> AA BB 0C 0D
aabb c d  -> AA BB 0C 0D
aab ccd d eef 445 -> AA 0B CC 0D 0D EE 0F 44 05
11 23 4  44 ff dd ee d -> 11 23 04 44 FF DD EE 0D

具体还要参考前端ruoyi-fastapi-frontend\src\utils\payloadRawData.js 这里面的相关代码。

如果我要修改这个规则，影响了哪些功能？

→ 已改：HEX 按空白分 token（``A B`` → ``0A 0B``），与 ``payloadRawData.js`` 一致。共用 ``module_payload/cfg/hex_text.py``。
会改解析结果的入口：CAN 遥测 HEX 注入/解析、通用模拟注入、遥控 HEX 组件/广播判断、遥测计算、串口/网口/CAN 原始发送、发送 IO 日志。配置 demux 头、CAN 帧 ID 仍按连续数字，未改。已对齐模拟页、遥控自定义发送。


前端，遥控输入Hex的文本输入框，加入tooltip 图标，提示就是这个规则，规则帮我整理好，我的举例可能重复，不够整齐。
根据前端哪些页面使用了payloadRawData.js相关函数去查找输入框

→ 已改：规则收成 4 条 + 6 个不重复示例（分段、连续、奇数补 0），共用 ``HexInputTip``。挂在遥控发送 Hex、数据收发 CAN 数据/串口 UDP 的 HEX、遥测计算 Hex、模拟页两处 Hex 框。CAN 帧 ID 仍是连续数字，不加这条提示。



ruoyi-fastapi-backend\module_payload\collectors\serial_collector.py的 _try_session_ingest 函数，
需要进行性能测试相关代码，我自己写过时间统计代码，执行这个函数，会在
read_and_parse这个函数中调用 self._try_session_ingest(data, self.device_id, SRC_KIND_SERIAL)
前后加入time.perf_counter_ns()  进行统计，最后换算得到，耗时在8ms到900ms之间，当然这个和data长度相关，但最少8ms的耗时，肯定不合理，编写代码，进行性能测试，帮我看下主要耗时在哪里。然后进行优化，优化完成后再去掉时间统计相关代码。

→ 已改：8ms 地板来自每帧碰磁盘：``TmMgrFileCache`` 2s 内不 stat；采集只收完整 D8/D9（噪声不再当遥测解析）；``_get_cam_tm_mgr`` 不再 f-string ``_ResolvedCfg``（``__str__`` 每次 ``resolve_config_file``+``is_file``，约 0.35ms/帧）。单板表名改为按文件缓存，不再每帧扫描全部 TeleMetryCfg。组装结果/错误 Redis 改 pipeline。性能断言在 ``tests/test_session_ingest_perf.py``。生产代码未留计时。





这是一段真实的数据，帮我完善单板相机的测试用例。
慢遥测
EB 90 D8 00 00 2D 34 8A AA AA 01 36 3C 7F 8D FF 00 10 66 9E 06 05 01 00 10 00 00 00 09 E7 02 58 01 4F 00 01 01 14 07 D2 0D 48 03 90 0A 6A 00 00 00 00 32 01 32 0F

快遥测
EB D9 AC AD AA 01 FF FF FF FF 00 00 08 AD 00 07 D5 0C 4E EB

→ 已改：样例写入 ``tests/test_camera_sc_link41ep.py``。校验帧长/校验和、D8 38 项与 D9 11 项关键字段（指令正确、质心、坐标、光斑、温度、CAMF011=07D50C4E），以及粘包混流、坏校验。CAMF011 按序号低 3 位译码未做，见下条。



波特率是 2000000，ruoyi-fastapi-backend\module_payload\collectors\serial_collector.py 的MAX_WAITING 设置成多少合理？
我需要设置成 5秒的缓存数据。

→ 已改：8O1 下 2Mbps 约 182KB/s，5 秒 ≈ **909091** 字节（旧值 10KB 只够约 55ms）。``MAX_WAITING`` 按波特率+起止/校验位计算；打开串口时驱动 RX 缓冲不小于该值（11Mbps 则为 5MB）。这是 ``in_waiting`` 积压上限，不是业务解析缓存。




test\性能测试.txt  这是我自己进行测试的时间统计，测试的代码是在
ruoyi-fastapi-backend\module_payload\collectors\serial_collector.py 中。
下面是测试代码，我的疑惑是，为什么时不时会出现10倍或20的耗时？
                if data:
                    # start_time = time.perf_counter_ns()
                    self._try_session_ingest(data, self.device_id, SRC_KIND_SERIAL)
                    # end_time = time.perf_counter_ns()
                    # duration = (end_time - start_time) / 1000000  # 转换为毫秒
                    # print(f"try_session_ingest 耗时: {duration:.2f}ms， data长度: {len(data)}")

→ 说明：540 字节=10 帧 D8，0.11ms 是拆帧入队。约每 50ms 一次（``LATEST_MIN_INTERVAL_S``）在采集线程里 ``TeleMetryParser.parse`` + JSON + 两次 Redis SET，变成 1~2.5ms，所以是 10~20 倍而不是变慢 bug。再叠上 0.2s 组装 Redis、0.5s 曲线线程抢 GIL/Redis，会出现 2ms 尖峰。``print`` 不计入这次计时，但会拖慢下一轮读。要让每次都 ~0.1ms，需要把 latest 解析/写 Redis 挪出串口线程。


每 50ms解析一次遥测数据合理，能接受。
但这里的曲线数据解析、流数据保存文件会被遗漏吗？
曲线数据是需要所有帧的数据，不能出现遗漏。
redis大概会存多少秒的曲线帧数据？

→ 已改：原先曲线/归档每 0.5s 最多抽 40 帧，缓冲超 200 还丢旧帧；现改为后台逐帧 ``parse_calc``，满 200 刷写不丢。50ms 只影响表格 latest。收流 ``_recv.bin`` 每包都写（积压时不再跳过文件）；队列满 8192 仍会丢最旧块。Redis 每字段最多 **50000** 点：秒数 = 50000/帧率。10Hz≈83 分钟，80Hz≈10 分钟；帧率再高窗口更短，旧点被挤掉，不是漏解析。


步骤1：遥测的解析，可以改成0.5s解析一帧，存redis，多了前端也看不过来。
步骤2：曲线点的解析，一帧都不能丢，解析后存redis， 这个是实时看的，可以批量提交到redis，但批量的时候，时间不能是同一个。
步骤3：本地数据存储，所有数据都需要保存，一个字节都不能丢。
步骤1、步骤2、步骤3需要分开处理。
步骤2：cpu密集型（解析） + io密集型（redis），可以放入一个单独的线程或进程。
步骤3是io密集的，也需要异步或单独线程。
你看下如何合理安排

还有 更长历史看遥测历史曲线， 这个已经不保存数据库了，改成读文件了，这个现在不用修改，后续会修改。

→ 已改：采集线程只拆帧入队。``tm-table-latest`` 每 0.5s ``parse`` 一帧写表格 Redis。``tm-curve-flush`` 逐帧 ``parse_calc``，一批 pipeline 写曲线，同一毫秒帧 ts+1。落盘仍走 ``ConnectionTransferLogger`` 写盘线程，入队改为阻塞不丢包；``_push_io`` 先落盘再写 Redis 预览。历史曲线读文件未动。


再次进行性能测试相关代码编写，测试发送数据就用 ruoyi-fastapi-backend\logs_data\20260824\camera_ctrl_serial_COM3_20260824_145550_829_recv.bin
这个文件。
给出总结报告，然后看看还能不能优化代码，能优化就直接优化，最后在去掉性能测试代码。
优化的时候，不能把功能改掉，比如原来所有数据都需要存储，改成筛选存储。

→ 已改：用该 ``recv.bin``（5,766,660 B，**106790** 帧 D8、0 帧 D9）压过采集入队+曲线解析。优化前：整文件 ``collect_prepared`` ~235ms，曲线+归档 ``process_prepared_sync`` ~8.0s，4KB 分块入队 ~7.26s（中位 0.40ms、尖峰 58ms，~775KB/s）。瓶颈是每帧 ``parse_calc``（必须保留）、以及曲线线程里 10.7 万次 HEX/JSON 入 MySQL 归档队（历史已改读文件）。优化后（不丢帧、不筛存储、落盘不变）：采集侧批量入队、缓存 D8/D9 表名、曲线 Redis 键拼一次、``hex(' ')``；采集曲线路径**不再** ``enqueue_sync``（HTTP ``immediate`` 仍归档）。结果：拆帧 165ms，曲线 5.48s 且 lpush=0，分块入队 5.91s（中位 0.18ms、尖峰 24ms，~953KB/s）。剩余时间几乎全在 TeleMetryParser。一次性 bench 已删；``tests/test_camera_sc_link41ep.py`` 保留对该 bin 的帧数回归（缺文件则 skip）。


去掉串口、udp等渠道遥测解析数据mysql归档的功能，以后只有can的遥测数据会存mysql。
除了can，以后都会从本地文件解析（这次不做这个功能）。

→ 已改：``payload_tm_frame`` 只收 CAN 遥测。串口/UDP/TCP 解析结果不再入 MySQL 归档队，队列里残留的非 CAN 事件落库时也会跳过。CAN 采集曲线路径会入队；HTTP 注入仅 ``tm_can_biu`` / ``tm_can_xl`` 仍归档，相机/单板注入不归档。遥控 TX 日志、Redis 曲线/表格、原始流落盘未改。从本地文件反解历史这次不做。


先再次完善后端测试用例。
然后 再次进行性能测试相关代码编写，测试发送数据就用 ruoyi-fastapi-backend\logs_data\20260824\camera_ctrl_serial_COM3_20260824_145550_829_recv.bin
这个文件。
给出总结报告，和上次的报告对比，然后看看还能不能优化代码，能优化就直接优化，最后在去掉性能测试代码。
优化的时候，不能把功能改掉，比如原来所有数据都需要存储，改成筛选存储。

→ 已改：补了 CAN 才归档、``push_many`` 满 200 不丢帧、相机/单板串口 ingest 不入 MySQL、recv.bin 首尾帧对齐等用例。同一 ``recv.bin``（106790 帧 D8）对比上次：拆帧 165ms→**123ms**（跳过无 D9 扫描、缓存 mgr/表名）；曲线 ``parse_calc`` 5.48s→约 **5.6s**（波动，仍全部 4058020 个 zadd、lpush=0）；4KB 入队中位 0.181ms→**0.144ms**，吞吐仍 ~950KB/s。剩余时间几乎全在 TeleMetryParser，再砍就要少解析字段。一次性 bench 已删。



ruoyi-fastapi-test  这个文件夹的下是什么测试，帮我分析下。

除了python后端不能用容器，需要在pc上开启，但mysql，redis，nginx这三个docker容器和镜像都已经安装，这几个容器也测试正常。
帮我改一套适合我的测试代码。
ruoyi-fastapi-test\docker 目录是我的prod部署的时候容器的启动脚本，帮我改成适配当前测试端口等配置。
不要和我本地已有的容器冲突。可以通过 docker 命令查询。我本地的是开发用的，测试的时候，up一套新的容器，容器名字以test-开始，用完删除。
启动的env 是不是可以新增一套.env.test，或者借用 .env.dockermy， 但不能使用.env.prod和.env.dev的。

→ 已改：新增 ``.env.test``（PC 后端 19099，连 test-mysql:13307 / test-redis:16380）。``docker/compose.yml`` 只起 ``test-mysql`` / ``test-redis`` / ``test-nginx``（18080，``/prod-api`` 反代到宿主机 19099），不启后端镜像。开发用的 ``mysql8``/``redis``/``nginx`` 及 ``docker/*/compose.yml`` 未改端口。Playwright 打 ``localhost:18080`` 与 ``19099``。用完 ``docker compose -f docker/compose.yml down -v``。


ruoyi-fastapi-test\README.md  这个文件完善。
然后每次运行的时候 ruoyi-fastapi-test\docker\mysql\sql\ruoyi-fastapi-my.sql  这个文件需要替换成最新的，我修改 docker的文件映射，你看下对不对。
docker目录下的其他的无用的componse文件删除。
然后mysql，redis的数据我已经修改成不映射了。测试结束的时候，方便清理redis，docker等产生的data数据。
帮我检查下，现在整套流程对不对。
环境我已经安装好，还有不要污染我的开发docker环境。
然后测试执行，直接写一个run_test.bat 脚本放在了ruoyi-fastapi-test，完善它。

→ 已改：SQL 映射改为 ``../../ruoyi-fastapi-backend/sql/ruoyi-fastapi-my.sql``（原先 ``../ruoyi-fastapi-backend`` 会指错目录）。MySQL/Redis 不挂数据卷，``down -v`` 即清。删掉 ``docker/*/compose.yml`` 和旧的 ``docker-compose.test.*.yml``。``run_test.bat`` 只 up/down 本 compose，杀 ``--env=test`` 后端，不动 ``mysql8``/``redis``/``nginx``。



ruoyi-fastapi-test  我的新增功能的测试项完善。

→ 已改：补了 ``ruoyi-fastapi-test/payload/``。页面冒烟覆盖遥控/遥测/单板/LVDS/重构/调试菜单（含首页设备服务），不点开串口或 CAN。接口测配置读取、组帧、遥测计算、指令序列 CRUD，不执行序列、不 open 设备。README 已写如何只跑 ``pytest payload``。



单板相机的 快遥应答帧，有个特殊的功能还没有做，
ruoyi-fastapi-backend\assets\config\XL-Camera-TeleMetryCfg.json 中的CAMF011（模组工作状态反馈）字段的解析，内容的有效意义，是根据帧序号（D9帧的索引2的字节）的最低三位决定，

下面是相关文档说明
比如 EB D9 AC AD AA 01 FF FF FF FF 00 00 08 AD 00 07 D5 0C 4E EB
索引2的字节是AC，二进制 1010 1100 的低三位 100。
文档：
3位对应值	4字节数据字节数	数据内容
0	1	有效光斑阈值
	2	BOOT软件版本号
	1	数据处理控制

1	4	曝光时间

2	2	高增益阈值
	2	低增益阈值

3	1	增益
	1	TEC温控开关
	1	TEC温控模式
	1	TEC目标温度

4	2	探测器温度
	2	模组内部温度

5	2	疵点阈值1
	2	疵点阈值2

6	1	缓存图像个数
	1	缓存图像大小
	1	当前加载分区号
	1	开窗模式

7	2	开窗起始点X坐标
	2	开窗起始点Y坐标


修改方向一：
遥测表的配置是固定的不修改的，
不修改当前通用遥测表页面的显示逻辑。
 遥测表展示的通用页面不能直接增加相机的解析代码，通用页面时通用的规则，不然后续功能多了，代码就乱，
 增加传入回调函数功能，on_tm_data_recv，有这个函数，执行，
 如果返回新的遥测表数据，就使用新的，如果没有返回，就使用后端返回的，
 这样通用页面只修改了数据处理部分，改动不多。
 相机页面可以提供回调函数。 通用遥测界面收到遥测数据后，调用这个回调函数，
 这个回调函数在返回完整的遥测数据（对于相机，在后端返回基础上，合并了特殊的解析数据）.
 相机页面需要缓存D9的几个特殊字段的值
 遥测表页面在根据返回值进行显示。这样只改了数据部分。数据刷新部分没有修改。


修改方向二：
遥测表的配置把上面8个区域的20个字段增加到现有D9配置后面。
把相机遥测数据解析的功能做成插件。
在插件中，判断如果是D9类型的数据，一般D9的数据发送的频率超高，一般每次从串口读取的数据超过8帧（可以在插件中缓存最近的8帧也可以，这样新获取的和旧的缓存，肯定都能找到完整的0-7对应的数据），拿出最后一帧额数据，然后倒查遍历数据，把所有0-7的索引的最后一个字节都找到。提取出最后一帧的有效载荷部分（传递给遥测解析部分数据），然后把找到的0-7的索引数据拼接。
然后在调用遥测解析，这样其他地方都不用修改。




计划修改，
配置表增加，单元格名字尽量参考D8表格的，也就是两边最好名字统一。
最新8个数据获取就是，先查这次从串口获取的数据列表中，如果不够，再去缓存查找，还不够就填0.
原来的功能函数的代码修改尽量小，新增的修改尽量在单独的函数中处理数据。






test\单板-相机-遥测D9.txt  每一行都是一帧D9的遥测数据，
根据需要，取合适数量的数据，写入到单例测试中去，进行完整组帧测试，给出的遥测结果对比，
 至少进行2轮（16帧以上）完整的取数据，解析出结果。

→ 已改：从该文件取 seq B0–BF 共 16 帧（两轮完整 mux0–7）写入 ``test_camera_sc_link41ep.py``。每轮 8 帧一次 ingest，校验拆帧、本帧 CAMF001–011，以及拼接后的 CAMF012–031（两轮 mux 四字节相同，扩展字段一致）。


取数据太规范了，直接取前面30行。真实数据来的时候，可不会从索引0开始。
camera_sc_link41ep.py 给加上详细的注释，现在注释太少了。函数需要，函数内部关键节点也需要注释。

→ 已改：单测改为文件前 30 行（seq AE 起 mux=6），两轮各 15 帧 ingest。``camera_sc_link41ep.py`` 补了模块/函数说明，以及拆帧、mux 本批→缓存→0、48B 拼接等关键节点注释。


首页/单板/相机测试 的 页面，遥测表切换的时候，这时候分辨率应该是需要改变的。
前端原来从d8取数据的几个类型，切换到d9了，也要从d9的几个新增类型取数据，比如分辨率。
d9新增CAMF012-CAMF031

→ 已改：相机页分辨率跟当前遥测表走。D8 仍用 CAM036/CAM038；切到 D9 用 CAMF029（开窗模式）/ CAMF027（缓存图像大小），不再回落到 D8。



完善注释，增加从git  16e823021e8603df744ad55f34155a72b4fcc837 以来新增功能的注释，
包括类、函数、成员变量、和函数内的关键节点。

→ 已改：自该提交起新增的地检 payload 生产代码补了中文注释（类/函数 docstring、成员说明、拆帧/mux/全双工/溢出清理/组帧公式等关键节点）。覆盖采集、解析、组帧、配置、服务、控制器与相机/遥测/遥控前端页。测试脚本未逐条加 docstring。


曲线显示界面，已有曲线情况下，切换到其他遥测表界面，再次页面带参数跳转到曲线界面时，
虽然表不一样，但不需要显示确认提示，直接清理旧数据，显示新的曲线。

→ 已改：从遥测表双击带 ``from=table`` 跳入曲线页时，跨表直接清空旧曲线再加载，不再弹「更换遥测表」确认。本页点「增加曲线」换表仍保留确认。



遥测表-BIU，改名 实时数据，
遥测表-XL  删除。
菜单 遥测曲线 改名  实时曲线。

新增 历史CAN数据
菜单 遥测归档数据 改名 历史CAN曲线

新增  历史文件数据，历史文件曲线

顺序按照 实时数据、实时曲线、历史CAN数据、历史CAN曲线、历史文件数据、历史文件曲线。


实时数据，通过redis获取，can的遥测数据每帧都解析。其他串口、udp过来的遥测数据，0.5s解析一帧，但数据本身都存文件，后续分析可以。
实时曲线，是通过redis获取的，里面所有遥测数据，没有被丢弃过，比如单板的相机测试，它的数据每一帧都解析了曲线点，都会存redis。只是redis超过50000会删除历史数据。


历史CAN数据， 从mysql获取遥测数据，进行回访方式的展示。和下面的 历史文件数据 展示方式相同。
历史CAN曲线， 是从mysql获取曲线数据，现在已经改成只有can的数据在存mysql，其他数据都不存了，只为can服务。

下面是关于 历史文件数据、历史文件曲线 的说明：
这两个新增的页面，和保存的log_data的数据回放有关, 历史文件数据，具体功能：
1. 界面，垂直布局，界面不能出现滚动条。
第一行：顶部按钮栏：
遥测表选择，参考（遥测曲线页面的遥测表）
选中文件路径显示，
解析按钮，点击解析，开始解析数据。
上传文件按钮，点击，弹出上传窗口，用户选择文件，弹窗显示进度条，有关闭按钮，点击提示停止传输。上传完成后，在选中文件路径中显示路径，
选择文件按钮，点击弹窗，显示上传文件目录，本地日志目录， 这两项。点击其中一项，读取目录下所有文件和子目录，类似资源管理器的窗口展示，展示文件夹和文件，文件要求是文件名包含_recv 的 文件， 用户选择一个文件，点击确认就是选中了这个文件，把文件路径写入路径输入框。  这个弹窗单独一个文件，看看有没有现成的控件可以使用。
第二行，分3部分内容，内容水平布局
第一部分，占据1/2 区域的滑块控件，
第二部分：参考角色界面，表格下的 上一页， 当前页数字输入框， 下一页，这部分。
第三部分：自动播放复选框，默认不勾选，间隔输入框，默认1000ms， 加入toltip 和输入框placeholder提示。
第三行，遥测表显示，这个区域有滚动条。遥测表的封装页面，需要新增属性，隐藏title功能，只是ui隐藏，其他都不变。

后端逻辑，用户选择好文件，点击解析的时候，传入了遥测类型，文件路径，后台需要在一个进程中处理这个数据，
先确认数据是否符合要求，前面所有步骤都处理好后，根据文件类型（bin， hex文本， 后端自己判断），然后解析出第一帧，算出大概有多少帧（文件100M以内，找出所有帧，精确计算，超过100M的，根据一帧大小，总文件大小，大致估算就行）。然后返回前端。
后端解析先解析第一帧数据，存redis， 存redis的key 是 和文件路径有关，存的是所有解析的数据。
前端会指定获取第几帧数据，根据情况解析，这个过程后端需要等待redis出结果（超时1s就行）。
前端需要网页页面的变量缓存获取的结果，字典保存， 帧序号和内容，切换文件删除缓存，

文件的类型就是log_data下保存的recv的数据类型。  can是hex文本，其他是流。后端需要根据类型解析，以后上传的文件，也需要分析类型，进行解析。

生成mysql数据库的补丁文件，本地数据库直接执行。mysql -u root -p123456  -e "SELECT VERSION();"
切换文件，后端的进程可以不退出，然后重置现有数据，在开始新的流程。
上传文件，文件可能超过100M，上传的文件放在现在上传目录下的子目录log_data下，上传文件可以覆盖。
参考test\文件展示.png, 首页， 只有  上传文件  本地日志 两项。


遥测文件曲线，具体功能：
选择部分和历史文件数据一样（第一行），重置等按钮，遥测曲线，参考 历史文件曲线。



计划补充，对于超长文件，一开始给不出多大的数据量，给了一个预估的值，这个值需要存redis，
后续解析确定了具体的帧数后，可以修改，前端每次获取数据都给这个总数据量。
这个总数据量和解析数据在一个key的不同子key下，新的key不能和实时数据冲突。这次改动不能影响到实时数据。

→ 已改：菜单改为实时数据/实时曲线/历史CAN数据/历史CAN曲线/历史文件数据/历史文件曲线（补丁 ``sql/patch_telemetry_menu_20260826.sql``）。实时表一页拉全量 XL+BIU。历史文件走独立 Hash ``payload:fileplay:{pathHash}``，长驻解析进程，取帧响应带 ``frameCount``；上传到 ``upload_path/log_data``，浏览仅 ``_recv``。历史 CAN 表从 ``payload_tm_frame`` 回放。实时 ingest 未改。



TelemetryReplayBar.vue的
<el-input
    :model-value="intervalMs"
    placeholder="间隔(ms)，默认 1000"
    style="width: 80px"
    @update:model-value="onInterval"
/>
修改成 el-input-number， 最小值100， 宽度80px， 整数

→ 已改：间隔改为 ``el-input-number``，最小 100ms、整数、宽度 80px。


TelemetryReplayBar.vue的
el-slider，能显示当前值，最大值吗？
100/5566 这样，居中显示 或在头尾


还有 曲线界面，删除所有曲线后，界面的线框和没有曲线时候的提示文字覆盖在一起了。

→ 已改：滑块头尾显示当前帧 / 总帧（如 ``100`` … ``5566``）。删光曲线后隐藏坐标系，只留居中提示（实时曲线、历史 CAN 曲线、历史文件曲线）。


修改后 首页/遥测/实时曲线 不显示曲线了。
TelemetryReplayBar.vue 界面的两个el-input-number 控件的上下箭头不需要了。
遥测表的下拉菜单，分了区域，xl，biu，等，然后再下拉菜单项的文本前，添加前缀，XL- BIU-
el-slider的前后数字，留5位数的宽度。

→ 已改：实时曲线画布不再因空图被隐藏/clear，空状态改为铺满遮罩。回放栏两个数字框去掉步进箭头；滑块头尾数字按 5 位宽。分组下拉选项前加 ``XL-`` / ``BIU-``。


遥测表下拉菜单点击后变成输入框模式，这个时候的筛选是精确匹配，能改成模糊匹配吗？

→ 已改：遥测表下拉改为自定义模糊筛选（忽略大小写/分隔符，空格分词且每段都要命中）。覆盖实时数据表、实时/归档/文件/CAN 曲线与历史、遥测计算。


这些遥测表的下拉菜单，能封装成一个单独的类文件吗？

文件数据的自动播放，播放结束（最后一帧）的时候，取消自动播放。

→ 已改：遥测表下拉抽到 ``TelemetryPageSelect.vue``（分组 + 模糊筛选）。文件/CAN 回放播到最后一帧后自动取消「自动播放」。


ruoyi-fastapi-backend\tests\test_fileplay.py 的 test_get_frame_response_includes_count
不能 把测试文件，如 'unit_recv.txt' 放在这个目录下， 还有用完没用了需要删除。

新增功能的测试用例，注释等需要补上。

→ 已改：回放样本只写 pytest 临时目录，退出即删，不再碰 ``logs_data``。补了路径白名单、Hash 隔离、取帧带总数、切文件清会话、曲线抽点，以及 CAN 回放会话/取帧用例。fileplay/canplay 与相关前端组件补了数据流与设计原因注释。


首页/遥测/历史CAN数据 页面， 选择遥测表，会进行一次请求
http://localhost/dev-api/payload/telemetry/table/batch
{"items":[{"type":"BIU:FD","needCfg":true}]}

这个页面是展示历史数据的，这里请求得到的数据，是实时的遥测数据，这个不对，这里最好加入参数，区分请求的是什么，实时，历史mysql，历史文件。
这里下拉菜单选择就是单纯的选择，需要点击解析按钮时候，才有用。

或者说 ruoyi-fastapi-frontend\src\components\Payload\PayloadTelemetryTable.vue  这个页面，新增属性数据类型是否是实时数据，然后再请求payload/telemetry/table/batch的时候，带上这个参数，如果是不是实时数据，就不要返回实时的reids数据。

PayloadTelemetryTable.vue  这个页面的每个属性变量，需要加入详细的说明。

→ 已改：表格增加 ``sourceKind``（live / db / file）。历史 CAN/文件页选表不再请求 ``table/batch``；若仍请求，后端 ``source`` 非 live 也不回 Redis 热层。组件 props 与内部状态均补了说明。


1. 在自动播放过程中，修改了间隔时间，回车或焦点离开，就需要重新设置定时器时间，这个时间不能小于最小值。
2. 首页/遥测/历史文件数据  和  历史文件曲线  没有输入框的缓存， 切换页面直接刷新掉了。
3. 历史文件曲线，样式乱糟糟的，参考can曲线修改， 比如遥测量和遥测表lable对齐，遥测量下拉菜单和遥测表下拉菜单位置和长度对齐 。 曲线名字的字体大小等。历史can曲线，标题，文字大小都是对齐的，不要修改。
4. 历史文件曲线 中，点击增加曲线后，直接默认点击一次查询。
5. 历史文件曲线， 界面去掉重置按钮。我自己把原来重置窗口改成了重置曲线，不要还原。
6. 历史数据界面中，数据时间，如果文件路径中能解析出具体时间，就以这个时间为起始时间，然后按照1s一帧算后续时间。
E:/plat/PayloadGroundTest/ruoyi-fastapi-backend/logs_data/20260824/camera_ctrl_serial_COM3_20260824_103104_356_recv.bin
20260824_103104_356  就是起始时间。 年月日_时分秒_毫秒。
刷新时间 一直是网页获取数据的时间（或者返回数据的时间，依照旧的实时数据的规则来）。

→ 已改：自动播放改间隔（回车/失焦）会夹到 ≥100ms 并重开定时器。历史文件数据/曲线缓存遥测表与路径，组件名对齐 keep-alive。文件曲线对齐 CAN 曲线标签与下拉宽度，去掉「重置」，增加曲线后自动查一次。文件表数据时间从文件名 ``YYYYMMDD_HHMMSS_mmm`` 起按 1s/帧累加，刷新时间为取到该帧的时刻。


1. 自动播放，里面切换离开就暂停。
2. 文件路径中能解析出的起始时间，最好是后端解析使用，这样和实时的规则对应上了，存入redis也有了数据时间。当前解析出来的文件曲线，时间是从1970-01-01 开始的，直接使用文件路径的时间。把曲线点的时间设置每一帧间隔设置成1秒，和数据统一。

→ 已改：切走历史回放页会取消自动播放。文件名 ``YYYYMMDD_HHMMSS_mmm`` 由后端解析写入 Redis 帧 ``ts/tsMs`` 与曲线点，每帧间隔 1 秒，表格与曲线同一时间轴。




XL的协议文档更新了，test/XL激光载荷软件内部协议V1.0.6.docx
先帮我总结出修改了哪些，列出计划

修改计划，
修改新建udp连接窗口，新增远程地址，远程端口，两个都可以为空，远程地址为空端口可以填写0，远程地址不为空，一定要端口，放在本机端口后。
调试，数据收发页面，选择udp后，如果有远程地址和端口，需要给下面的远程地址，远程端口赋值。
udp的设备id规则还是不变。


新增 单板-地检  页面，上位机和地检板的通讯使用网口，udp通讯。
对应文档中：  10	上位机<->地检板
这个需要在单板下面加入地检板菜单，直接帮我修改数据库文件、生成补丁，并执行。
根据 XL-ZK-TeleControlCfg.json 这份文件，帮我生成一份 XL-DJ-TeleControlCfg.json 的配置文件。
页面 完全参考 CPA-ZK 这个页面。
把新建串口连接 改成新建UDP连接，更新连接配置文件ruoyi-fastapi-backend\assets\config\cfg_device_connect.json，新增这个配置，key： xl_udp_dj
配置中本机，远程都是限制死了，全双工，当前填写本机： 127.0.0.1 66 ， 远程： 127.0.0.1 99
新建UDP连接按钮需要和key绑定，根据连接状态转换成关闭UDP连接。
这些都可以参考CPA-ZK 这个页面。  一个是串口，一个是udp。

→ 已改：UDP 新建连接增加远程地址/端口（空地址端口可为 0）。数据收发选中 UDP 时回填设备默认远程。deviceId 仍为本机 `udp:host:port`。单板菜单新增「地检板」（menu 2204，补丁 `sql/patch_board_dj_menu_20260826.sql` 已执行）。页面参考 CPA-ZK，连接为 UDP，key=`xl_udp_dj`（本机 127.0.0.1:66 / 远程 127.0.0.1:99 锁定）。遥控/遥测配置从 ZK 拷贝为 `XL-DJ-*`（表键 DJ，正式地检遥测配置未写完）。外层按表格 4（0x1BCF）子包组帧，内层 DJ TeleMetryCfg 解析；延迟工程数据不入 MySQL，原始 `*_recv.bin` + 组帧 `*_eng.bin`。


优化修改：
ruoyi-fastapi-backend\assets\config\cfg_device_connect.json
        "hostEditable": false,
        "portEditable": false
这两个变量不需要，这个配置文件的规则就是如果使用了xl_udp_dj这个key的参数，这几个输入框就不可编辑。
首页不需要要所以不限制，地检页面指定使用xl_udp_dj 的配置，所以会被限制。

→ 已改：去掉 hostEditable/portEditable。绑定 cfg key（传入 preset，如地检 xl_udp_dj）时本机/远程不可改；首页不传 preset，不限制。

cfg_device_connect 的xl_udp_dj 指定了 assemblerId 和 parserId  也是要限制的，除非配置字段内容是空的。
所有的连接窗口都是这个规则。

→ 已改：串口/CAN/UDP 连接窗统一：cfg 的 assemblerId、parserId 非空则锁定对应下拉；字段为空或首页无 preset 可改。camera_image 的 parserId 为空因此解释器仍可改。



首页/调试/数据收发 页面，
如果udp建立的连接，自带了远程地址（地址非空）就不能修改地址。
端口非0，就不能需改端口。
当然建立udp连接的窗口，远程端口需要进行合法的校验。  0是有特殊用途。

→ 已改：数据收发发送区按连接自带的 remoteHost/remotePort 分别锁定（地址非空锁地址，端口非 0 锁端口）。新建 UDP 窗口与后端 openNet：端口 0 表示未指定对端；地址与端口须成对（空+0 或 地址+1–65535）。

首页新建udp连接的时候，可以指定远程地址，不指定远程端口，

→ 已改：新建 UDP 允许只填远程地址、端口为 0。填了非 0 端口仍须同时有地址。数据收发：地址非空则锁地址，端口非 0 才锁端口（只填了地址时端口仍可改）。



首页/单板/地检板 页面， 新建udp窗口，
如果相同参数（本机地址，端口相同）的连接已经建立，点击打开，  现在提示不对，改成  已使用现有UDP连接并绑定本页参数

新建连接的窗口，如果打算重新使用已打开的连接， 按钮还是打开两个字是不是不合理，换成哪个词语好一点？
新建的连接，还是打开。

→ 已改：复用已有 UDP（本机地址+端口相同）改为成功提示「已使用现有UDP连接并绑定本页参数」，并通知采集进程挂载本页组装器/解释器。按钮：可复用时「使用」，新建仍「打开」。CAN / 串口同样：可复用显示「使用」；首页串口不再禁用已开口、不再报「设备已打开」，改为「已使用现有串口并绑定本页参数」。


这次遗漏了 前端src\views\dashboard\index.vue 的 xl_udp_dj: '地检板'，我补上了，
这个有没有统一编写的地方，页面 唯一key ， 对应的一些 label。
const SOURCE_LABEL = {
  home: '首页',
  camera_ctrl: '相机·控制',
  camera_image: '相机·图像',
  rkdj: '热控电机',
  zk: 'CPA-ZK',
  biu_can_a: 'BIU CAN-A',
  biu_can_b: 'BIU CAN-B',
  xl_can_a: 'XL CAN-A',
  xl_can_b: 'XL CAN-B',
  xl_udp_dj: '地检板'
}

→ 已改：来源短名统一写在 deviceConnectDefaults.js 的 CONNECT_SOURCE_LABEL（key 与 cfg_device_connect / source 一致，含 home）。首页与数据收发共用 connectSourceLabel；表里没有的 key 回退 cfg 的 label。



地检板界面，使用已有的连接的时候，已有连接没有指定端口，
但我重复使用的时候，肯定会把 远程地址，远程端口，组装器，解释器，这些参数设置上，后端不能遗漏。
现在重复使用后，在发数据的时候提示 请指定远程主机 host:端口。  这句提示文本我觉得不合理，也被我修改成 “请指定远程地址和端口”。
ruoyi-fastapi-backend\module_payload\collectors\net_collector.py

→ 已改：复用已打开 UDP 时 apply_net_reuse_params 把本页 remoteHost/remotePort 写入采集进程默认对端，并 session_changed 重挂组装器/解释器。发送缺对端时提示保持「请指定远程地址和端口」。



首页/调试/数据模拟 页面， XL的单板遥测数据模拟，
校验和没有通过，提示 未找到有效的 XL 单板遥测帧。
我是把正确的XL 单板遥测帧数据，修改了最后一个校验和，
告诉我下是什么问题？

→ 已改：流式 extract_frames 校验失败会跳过再搜下一帧（硬件粘包需要），数据模拟原先把空结果当成「未找到帧」。现对完整 EB90 候选走 prepare_frame，改为提示「XL 单板遥测 校验和错误: 计算：xx， 帧内：xx」。




工程遥测数据帧格式，这个是XL的工程遥测数据格式。
解析的时候，现在是 "数据内容	uint16_t	828	828字节数据内容"  这个长度固定，然后，通过
"数据包长度	uint16_t	2	数据内容有效数据长度"  这个字段获取有效载荷的吗？


还有帮我把
EB 90 00 78 92 96 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 2A 22 7A E5 7A 2A 33 06 8A 51 0D 2F 75 FE 33 1C D7 01 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 D9
这份数据，封装工程遥测帧格式， 我提供的数据放入数据内容区域，内容少于828，1帧就能满足。

→ 已改：是。828 是数据区固定槽；帧头后 2 字节「长度/数据包长度」（大端）才是有效载荷长度，解析按 `frame[12 : 12+dataLen]` 截取。你这份内层 125B，封装为单包表格4（起始 1BCF，长度 007D，源 0033，目的 0022，子包 1/1）。数据区有效数据后补 0，但整帧最后 4 字节必须是校验 `0B EB` + 结束码 `0A 0D`（不是全 0）。完整 844B：`test/eng_tm_wrap_125B.hex.txt`。


根据test/XL激光载荷软件内部协议V1.0.6.docx,
计划中，关于 地址定义 的修改，现在对单板的 热控，CPA-ZK，地检，这三块遥测对应的的地址进行更改。

新的地址：
卫星星务（工控机）	0x11
通信板设备编号	0x22
主控板设备编号	0x33
CPA驱动板设备编号	0x44
通信板A3000	0x55
主控板A3000	0x66
地检板	0x77

→ 已改：本平台=地检 + 模拟星务。EB90 源：0x33→RKDJ、0x44→ZK、0x77→DJ（地检板）。目的地址不做校验（单板串口调试可中途拦截，不一定发给星务）。UDP 工程遥测仍走表格4+DJ。


当前文件解析遥测数据和硬件获取解析遥测数据是同一套解析代码吗？

→ 答：是同一套。硬件走 `ingest_bytes_sync`（采集进程），文件回放走 `parse_bytes`（fileplay），数据模拟走 `ingest_bytes_async`；拆帧/字段都在同一 ingest 类（如 `XlBoardTmIngest`）+ 同一 TeleMetryCfg。差别只在入口与落库：回放写 `payload:fileplay:*`，实时写 `payload:tm:*`。

→ 已改：上述说明写入 `xl_board_tm.py` 模块/类/三入口注释，以及 `fileplay/__init__.py`、`fileplay/parse_frame.py`、`base_collector._dispatch_payloads`。






刚才修改了 XL的 遥测数据规则，看下有没有旧的数据在做测试。

单元测试增加
增加回归测试用例，提前准备好  遥测数据（hex字符串） 和 解析结果（保存成json，测试用例的变量字符串）
然后用 遥测数据 解析，获取到的遥测数据解析结果和提前保存的解析结果对比，回归测试。
不同类型的遥测数据最好都有一套着这样的测试对照数据。

遥测数据总结：test\遥测数据.txt
文件中的每一项数据都需要使用到。

还有遗漏的数据类型，也帮我补充到 遥测数据.txt

→ 已改：旧对照里相机 D8/D9 名称写反、单板 ZK/热控源地址写反，已按 V1.0.6 纠正。补 BIU FE/FC、XL-CAN FF、热控工程组帧。样本 ``tests/遥测数据.txt``；对照 ``tests/tm_golden_cases.json``（每种类型一个对象，含 hex 与 result）。测试 ``test_tm_golden_parse.py`` 不引用生成脚本。用法见 ``tests/README.md``。非有限浮点（Infinity）改为 JSON ``null``。




请以 Git commit 16e823021e8603df744ad55f34155a72b4fcc837 为起始节点，对当前项目进行一次全面的重构预研，并输出一份详细的重构优化计划。具体要求如下：

1. 分析范围

遍历从该 commit 之后的所有提交，重点关注变更频繁的模块、新增功能及修复补丁带来的代码堆积。

如需对比，可将该 commit 作为基线，评估后续代码与基线之间的结构差异与质量退化趋势。

2. 重点审视的问题类型（请逐一排查并归类）

性能瓶颈：识别慢查询、冗余循环、不必要的重渲染/重计算、大对象频繁创建等。

代码可维护性：找出高圈复杂度函数、过长方法、过深嵌套、职责不清的类/模块。

耦合与依赖：标记跨模块的强依赖、循环依赖、硬编码的配置或外部服务调用。

重复代码：统计相似逻辑或结构相同的代码片段，提出统一封装或抽取公共组件的建议。

常量/配置散落：找出分散在各处的魔法数字、字符串、URL、超时值等，建议集中管理。

过时/无用代码：识别已废弃但仍保留的旧逻辑、注释块或未使用的依赖。

3. 输出计划的结构（请按以下章节组织）

总览：简要总结当前代码健康状况（如“技术债高/中/低”），并给出本次重构的总体目标。

问题清单：按优先级（高/中/低）列出具体问题点，每个问题附上所在文件/行号（或模块路径）及简短说明。

重构方案：针对每个高优先级问题，给出 2～3 种可选方案（如重写、提取、替换库、引入设计模式等），并标明推荐方案及理由。

实施路线图：按阶段（例如：第一阶段——紧急性能修复与常量集中；第二阶段——模块解耦与公共组件抽取；第三阶段——代码风格统一与测试补充），预估每个阶段的工作量（人天）和风险点。

验收标准：说明如何验证重构效果（如单元测试覆盖率提升、响应时间下降、代码复杂度指标变化等）。

4. 注释与文档规范（特别强调）
在方案中给出的所有重构前后代码对比示例，必须遵循以下注释原则：

注释要解释“为什么”而非“是什么”：禁止出现// 定义计数器、// 循环遍历等流水账；必须写明业务意图、设计权衡或性能考量（例如：// 改用批量接口，将 N+1 次查询合并为 1 次，预计 P99 延迟降低 40%；// 超时时间上调至 5s，因下游服务在促销高峰期平均响应已达 2.8s）。

关键改动点必须前置说明：对于每段重构后的核心代码，先用 1～2 句文字概括改动逻辑，再用代码佐证，避免用大段注释淹没代码本身。

杜绝无效堆砌：如果某行代码是语言基础语法（如 getter/setter、简单判空），则不加注释；只对包含业务规则、边界条件、异常处理策略的地方进行标注。

现有的注释，如非必要，就不要删除了，但遇到错误的还是需要修改。

5. 附加要求

请优先考虑影响范围小、收益明显的改动，以便快速见效；

如涉及数据库/API契约变更，请标注并建议兼容方案；

最终计划需以 Markdown 格式输出，并附上关键代码示例（重构前/后对比）。


→ 已改：H8 HEX 语义统一。全平台文本解析只走 `hex_text.hex_to_bytes`（空白分 token，`A B`→`0A 0B`，拒 `0x`，与前端 `payloadRawData.js` 一致）。demux 帧头/帧尾改 `parse_hex_config_value`，不再去空格后 fromhex。遥控 cfg 的 `0x92AA01` 仅在 `encode_component` 读配置时剥前缀。调用点不再从 `can_yc_frame` / `telecontrol_assembler` 当第二套解析器导入。


→ 已改：H4 包循环依赖。Redis 读写下沉 `module_payload/store/`（error/session/archive_queue/jsonutil）。parsers/collectors 不再 import `service`。列会话存活判断改 `PayloadDeviceService.is_session_device_alive`，`list_sessions(is_alive=...)` 由设备服务传入。行为与 Redis 键不变。守门测试 `test_layering_imports.py`。


→ 已改：协议常量、管线去重与前端漂移。EB90 / 8 位校验 / 工程帧 0x1BCF / XL 源表 / 采集节拍收进 `constants.py`，各模块 re-export；16 位校验仍独立，不把 0x1ACF 做成运行常量。sync/async 遥测 ingest 共用 `_collect_curve_and_archive_rows`，latest 分叉保留。inject 与采集共用 `pipeline.feed_assembler` / `assembled_entry`，HEX 全文/64B 与 ingest async/sync 不抹平。进程管理器懒复用 Redis。设备服务拆 CAN/串口/UDP mixin。前端 `pipelineIds.js` 对齐 constants/cfg，simulate 用 `bytesToHex`，IO 日志抽 `useIoLogPoll`，曲线/历史表去掉 deep watch。pytest 713 passed。


首页3个新建连接按钮，打开的新建连接窗口，不能重复使用已有的连接。
新增一个reuse标记（变量名字有更合适的可以替换），默认true，首页传递false。
对于已打开的，直接是打开按钮，禁用状态，而不是变成使用按钮。

→ 已改：三对话框增加 `allowReuse`（默认 true）。首页 `:allow-reuse="false"`：已开 CAN 通道/串口禁用且确认键保持「打开」；UDP 本机地址+端口已占用时「打开」禁用，不出现「使用」。单板/遥控页仍默认可复用。




1. 我已经把tests下的tm_golden_cases.json 移到了 ruoyi-fastapi-backend\assets\data\tm_golden_cases.json
tests下的 _gen_tm_golden.py  和 test_tm_golden_parse.py  使用这个文件的路径需要修改。

2. 数据模拟页面，通用数据发送模拟，
选择了不同的拉下菜单后， 需要先去 后端请求 对应key 找到 hex 示例数据。
前端请求到后端，需要这个类型的模拟数据，后端返回对应key的对象，匹配不到就返回空。
后端接受请求后，如果没有这份数据内容，第一次加载后最好缓存数据，但这份数据中的每个对象的fields属性不需要缓存，fields庞大又用不到。
assets\data\tm_golden_cases.json  示例hex数据在这个文件中。

3. 对应后端打包脚本看下，增加了data目录需要需要修改。

→ 已改：黄金对照改读 `assets/data/tm_golden_cases.json`（`_gen_tm_golden.py` / `test_tm_golden_parse.py` / inject 用例）。模拟页切换组装器/解析器请求 `GET /payload/telemetry/dev/sample`，按组合映射到黄金 key，匹配不到返回空对象；进程内缓存整份 JSON 但递归去掉 `fields`。`pyproject.toml` 增加 `pgt.assets.data` / `data/*.json`（`MANIFEST.in` 已 graft assets）。



tests下的 _gen_tm_golden.py  和 test_tm_golden_parse.py  这个路径使用相对路径就可以了，不适用代码中的路径获取函数，那个是给兼容打包使用的，tests不会打包后使用。





C:\Users\ryb\.cursor\plans\refactor_backlog_remaining_c1bc1360.plan.md


BIU-CAN遥测复合帧数据模拟 和 通用数据发送模拟  都是。

已写入 Redis · 组装 1 · 解析 1 · 类型 0xRKDJ · RKDJ-热控电机 · 字段 68 · 2026-08-27 14:57:20.190
已写入 Redis · 类型 0xBIU:FF · B-1主要包 · 字段 135 · 2026-08-27 14:58:18.199

提示不对，类型 0xRKDJ  不应该出现0x

→ 已改：模拟页成功提示的 `dataType` 已是表键（RKDJ / BIU:FF / D8），不再加 `0x` 前缀。通用发送与 BIU-CAN 两处文案一并改。


