# 04 - 后端接口设计

新增业务模块 `module_payload`，遵循 RuoYi-FastAPI 分层规范
（`controller → service → dao → entity(do/vo)`），路由用 `APIRouterPro` 自动注册。

- 统一前缀：`/payload`
- 统一鉴权：路由级 `PreAuthDependency()`（登录态）；按钮级权限用 `UserInterfaceAuthDependency('payload:xxx:yyy')`
- 统一返回：`ResponseUtil.success(...)`，分页 `PageResponseModel[T]`

---

## 1. 控制器划分

| 控制器                     | 前缀                    | 职责                                       |
| -------------------------- | ----------------------- | ------------------------------------------ |
| `device_controller`        | `/payload/device`       | CAN/串口/网络的连接管理、通道列表、状态     |
| `telecontrol_controller`   | `/payload/telecontrol`  | 遥控配置（`family=biu\|xl`）、下发、控制开关 |
| `telemetry_controller`     | `/payload/telemetry`    | 实时/历史/文件回放遥测、曲线、开发注入       |
| `sequence_controller`      | `/payload/sequence`     | 指令序列 CRUD、复制、执行                   |
| `camera_controller`        | `/payload/camera`       | 相机图像采集与遥控（`protocol=v16\|v17`）   |
| `xl_board_controller`      | `/payload/xl-board`     | 单板遥控组帧/下发（RKDJ/ZK/DJ）             |
| `config_file_controller`   | `/payload/config-files` | 遥控/遥测 JSON 在线编辑                   |
| `lvds_controller`          | `/payload/lvds`         | 工程遥测（LVDS）                            |

---

## 2. 设备连接管理 `/payload/device`

| 方法 | 路径 | 说明 | 权限 |
| ---- | ---- | ---- | ---- |
| GET | `/payload/device/can/vendors` | 列出 CAN 厂商 | `payload:device:list` |
| GET | `/payload/device/can/list` | 列出可用/已开 CAN 通道 | `payload:device:list` |
| POST | `/payload/device/can/open` | 打开 CAN（可带 `parserId`/`assemblerId`/`source`） | `payload:device:open` |
| POST | `/payload/device/can/close` | 关闭 CAN 通道 | `payload:device:open` |
| GET | `/payload/device/serial/list` | 列出系统串口 | `payload:device:list` |
| GET | `/payload/device/serial/opened` | 列出已打开串口 | `payload:device:list` |
| POST | `/payload/device/serial/open` | 打开串口（启动采集；可绑组装器/解释器/`source`） | `payload:device:open` |
| POST | `/payload/device/serial/close` | 关闭串口 | `payload:device:open` |
| GET | `/payload/device/net/addresses` | 列出本机地址 | `payload:device:list` |
| GET | `/payload/device/net/opened` | 列出已打开网络连接 | `payload:device:list` |
| POST | `/payload/device/net/open` | 打开 UDP（可绑组装器/解释器/`source`） | `payload:device:open` |
| POST | `/payload/device/net/close` | 关闭网络连接 | `payload:device:open` |
| GET | `/payload/device/io-log` | 查原始收发日志（`deviceId` 可为 `serial:…` 或 `source:…`） | `payload:device:list` |
| DELETE | `/payload/device/io-log` | 清空 IO 日志 | `payload:device:open` |
| GET | `/payload/device/snapshot` | 聚合快照（can/serial/net/sessions/parsers/assemblers…） | `payload:device:list` |
| GET | `/payload/device/status` | 查询某设备状态（Redis） | `payload:device:list` |
| GET | `/payload/device/parsers` | 列出可用解释器 | `payload:device:list` |
| GET | `/payload/device/assemblers` | 列出可用组装器 | `payload:device:list` |
| GET | `/payload/device/sessions` | 列出已打开设备会话 | `payload:device:list` |
| POST | `/payload/device/bind-parser` | 绑定/解绑解释器；`updateAssembler=true` 时可写组装器 | `payload:device:open` |

> 打开类接口均可带 `parserId` / `assemblerId` / `source`；首页 `source=home`，单板写功能来源 id。

### 打开 CAN 通道

`POST /payload/device/can/open`

```json
{
  "vendor": 2,
  "devIndex": 0,
  "canIndex": 1,
  "baudRate": 500,
  "nodeAddrTo": 13,
  "cableFlag": 0,
  "parserId": "tm_can_yc",
  "assemblerId": "passthrough",
  "source": "home"
}
```

返回：

```json
{ "code": 200, "msg": "操作成功", "data": { "deviceId": "can:2:0:1", "status": "opened" } }
```

> 行为：管理器按「厂商+设备索引」聚合——进程若已存在则追加通道；否则新建进程。通道标识为 `can:{vendor}:{devIndex}:{canIndex}`。
> 关闭时若该卡所有通道关闭则回收进程（见 [02 章进程管理](./02-数据采集层设计.md)）。
> 首页打开时 `source=home`；单板页打开串口时写 `camera_ctrl` / `rkdj` 等，供 IO 双写与列表「来源」列展示。

### 设备状态

`GET /payload/device/status?deviceId=can:0:0:1`

```json
{
  "code": 200,
  "data": {
    "deviceId": "can:0:0:1",
    "connected": true,
    "lastHeartbeat": "2026-06-22 17:00:00",
    "stats": { "rx": 12345, "tx": 67, "errRate": 0.0 }
  }
}
```

---

## 3. 遥控 `/payload/telecontrol`

| 方法 | 路径                              | 说明                                          | 权限                       |
| ---- | --------------------------------- | --------------------------------------------- | -------------------------- |
| GET  | `/payload/telecontrol/config`     | 读取遥控配置；`family=biu\|xl`（默认 biu）   | `payload:telecontrol:view` |
| GET  | `/payload/telecontrol/order/{id}` | 取单条指令定义（组件、参数）                  | `payload:telecontrol:view` |
| POST | `/payload/telecontrol/assemble`   | 由组件值组装 HEX（后端校验/预览，可选）       | `payload:telecontrol:view` |
| POST | `/payload/telecontrol/send`       | 下发单条遥控指令                              | `payload:telecontrol:send` |
| GET  | `/payload/telecontrol/history`    | 取发送历史（来自 Redis）                      | `payload:telecontrol:view` |
| POST | `/payload/telecontrol/control/op` | 控制开关页操作（串口/定时遥测/校时/统计等）   | `payload:control:view`     |

### 读取遥控配置

`GET /payload/telecontrol/config?family=biu` → `BIU-TeleControlCfg.json`；`family=xl` → `XL-TeleControlCfg.json`。  
返回结构化的「页 → 指令 → 组件」树。`number` 组件可带 `formula`（组帧前对 UI 值求值，见 [07](./07-遥控帧组装与遥测解析规则.md)）。

### 下发单条指令

`POST /payload/telecontrol/send`

```json
{
  "deviceId": "can:0:0:1",
  "orderId": "K1502",
  "hex": "0A 91 00 04 00 04 AA AA",
  "broadcast": false,
  "appendChecksum": false
}
```

- `hex` 可由前端组装后传入，或仅传 `orderId + 组件值` 由后端用 [07 章规则](./07-遥控帧组装与遥测解析规则.md) 组装；
- 后端把指令 `LPUSH` 到 `payload:{deviceId}:cmd`，并轮询 `:cmd:result:{cmd_id}` 返回执行结果；
- 广播帧 `all_channel=true`（见 07 章）。

返回：

```json
{ "code": 200, "data": { "cmdId": "uuid", "success": true, "message": "OK" } }
```

### 控制开关操作

`POST /payload/telecontrol/control/op`（`op` 区分动作，对应 C++ PayloadControl 行为）：

| op                | 入参                                  | 含义                          |
| ----------------- | ------------------------------------- | ----------------------------- |
| `timedYc.enable`  | `{ enable: true }`                    | 定时遥测开/关                 |
| `timedYc.param`   | `{ dataCode: "F9", intervalMs: 1000 }`| 设置定时遥测类型与间隔        |
| `ppsTime.enable`  | `{ enable: true }`                    | 原子钟校时开/关               |
| `ppsTime.start`   | `{ utc: "2026-06-08 09:42:57" }`      | 时间同步起始时间（UTC）       |
| `ppsTime.offset`  | `{ offsetMs: 0 }`                     | 时间同步偏移                  |
| `rate.start`      | `{ durationSec: 600 }`                | 开始通信速率统计              |
| `rate.stop`       | `{}`                                  | 停止统计                      |
| `customSend`      | `{ hex, appendChecksum }`             | 自定义 HEX 发送               |

> 这些操作多为「下发固定/参数化 CAN 帧」或「调用采集进程能力」。具体帧定义参考
> C++ `PayloadControlWidget`（见探查报告），实现时落到 [07 章](./07-遥控帧组装与遥测解析规则.md)。**部分时间换算待确认**。

---

## 4. 遥测 `/payload/telemetry`

| 方法 | 路径                                   | 说明                                         | 权限                      |
| ---- | -------------------------------------- | -------------------------------------------- | ------------------------- |
| GET  | `/payload/telemetry/pages`             | 聚合全部遥测表页（BIU/XL/单板/相机，`family`）| `payload:telemetry:view`  |
| GET  | `/payload/telemetry/config`            | 读取指定表定义（`type` + 可选 `family`）     | `payload:telemetry:view`  |
| GET  | `/payload/telemetry/table`             | 取某遥测表最新一帧解析结果                   | `payload:telemetry:view`  |
| GET  | `/payload/telemetry/fields`            | 取某遥测表的遥测量列表（曲线下拉用）         | `payload:telemetry:view`  |
| GET  | `/payload/telemetry/curve/data`        | 拉取某遥测量的曲线数据点                     | `payload:telemetry:curve` |
| POST | `/payload/telemetry/curve/data/batch`  | 批量拉取实时曲线数据点                       | `payload:telemetry:curve` |
| POST | `/payload/telemetry/history/curve/batch` | 批量拉取 MySQL 归档曲线点                  | `payload:telemetry:archive` |
| POST | `/payload/telemetry/history/frames/open` | 打开历史 CAN 表回放会话                    | `payload:telemetry:canHistory` |
| GET  | `/payload/telemetry/history/frames`    | 取历史 CAN 第 N 帧                           | `payload:telemetry:canHistory` |
| POST | `/payload/telemetry/file/upload`       | 分片上传日志到 `log_data`                    | fileHistory / fileCurve   |
| GET  | `/payload/telemetry/file/browse`       | 浏览 upload / logs 目录                      | fileHistory / fileCurve   |
| POST | `/payload/telemetry/file/parse`        | 子进程拆帧（独立 Redis 状态）                | fileHistory / fileCurve   |
| GET  | `/payload/telemetry/file/status`       | 文件解析进度                                 | fileHistory / fileCurve   |
| GET  | `/payload/telemetry/file/frame`        | 取文件第 N 帧                                | fileHistory / fileCurve   |
| POST | `/payload/telemetry/file/curve`        | 文件帧曲线点列                               | `payload:telemetry:fileCurve` |
| POST | `/payload/telemetry/dev/can-yc`        | 开发：注入 CAN 遥测复合帧 HEX                | `payload:devtest:view`    |
| POST | `/payload/telemetry/dev/pipeline`      | 开发：HEX → 组装器 → 解释器 → 写 Redis       | `payload:devtest:view`    |
| GET  | `/payload/telemetry/dev/samples`       | 模拟页黄金样本列表                           | `payload:devtest:view`    |
| GET  | `/payload/telemetry/dev/sample`        | 模拟页黄金样本 HEX                           | `payload:devtest:view`    |
| POST | `/payload/telemetry/calc`              | 单字段 Hex → `parse_line_hex`                | `payload:tmcalc:view`     |
| GET  | `/payload/telemetry/calc/history`      | 遥测计算历史（Redis，最多 100）              | `payload:tmcalc:view`     |
| DELETE | `/payload/telemetry/calc/history`    | 清空遥测计算历史                             | `payload:tmcalc:view`     |

> 曲线数值优先使用解析字段 `calc_val`（公式后、值映射前）。文件回放实现：`module_payload/fileplay/`。

### 取遥测表最新值

`GET /payload/telemetry/table?deviceId=can:0:0:1&type=FF`

```json
{
  "code": 200,
  "data": {
    "type": "FF", "name": "B-1主要包", "ts": "2026-06-22 17:00:00.123",
    "rows": [
      { "id": "JGB001", "name": "遥测请求指令计数", "value": 2, "show": "2", "unit": "", "hex": "02" },
      { "id": "JGB103", "name": "终端模式", "value": 0, "show": "待机模式", "unit": "", "hex": "00" }
    ]
  }
}
```

> 前端遥测表页按固定间隔轮询该接口刷新（建议 500ms~1s，可配置）。
> 数据由采集进程就地解析后写入 `payload:{id}:tm:{type}`，主进程只读 Redis。

### 曲线数据

```mermaid
sequenceDiagram
  participant FE
  participant API
  participant R as Redis
  participant Col as 采集进程
  Note over Col,R: 帧到达后全字段 ZADD（无需订阅）
  loop 定时
    Col->>R: ZADD payload:{id}:curve:FF:JGB027 score=ts member=ts|val
    FE->>API: POST curve/data/batch 或 GET curve/data
    API->>R: ZRANGE / ZRANGEBYSCORE
    API-->>FE: [{t, v}, ...]
  end
```

`GET /payload/telemetry/curve/data?deviceId=...&type=FF&field=JGB027&limit=600`

```json
{ "code": 200, "data": { "field": "JGB027", "name": "APD温度", "unit": "°C",
  "points": [ {"t": 1750000000123, "v": 28.42}, {"t": 1750000001123, "v": 28.45} ] } }
```

---

## 5. 指令序列 `/payload/sequence`

标准 RuoYi CRUD + 复制 + 执行。对应表 `payload_cmd_sequence`（见 [03 章](./03-数据库设计.md)）。

| 方法   | 路径                          | 说明                                | 权限                     |
| ------ | ----------------------------- | ----------------------------------- | ------------------------ |
| GET    | `/payload/sequence/list`      | 分页查询                            | `payload:sequence:list`  |
| GET    | `/payload/sequence/{seqId}`   | 详情                                | `payload:sequence:query` |
| POST   | `/payload/sequence`           | 新增                                | `payload:sequence:add`   |
| PUT    | `/payload/sequence`           | 修改                                | `payload:sequence:edit`  |
| DELETE | `/payload/sequence/{seqIds}`  | 删除（支持批量）                    | `payload:sequence:remove`|
| POST   | `/payload/sequence/{seqId}/copy` | 复制（返回填充好的草稿，不落库）  | `payload:sequence:add`   |
| POST   | `/payload/sequence/{seqId}/run`  | 执行序列（按 interval 顺序下发）  | `payload:sequence:edit`  |

### 复制语义

`POST /payload/sequence/{seqId}/copy` → 返回源序列内容副本（`seqName` 加「-副本」后缀、清空
`seqId`），前端跳转到「新增」表单并预填，由用户确认后再提交保存（见 [05 章](./05-前端页面设计.md)）。

### 执行序列

`POST /payload/sequence/{seqId}/run`，入参 `{ deviceId }`。后端按 `commands` 顺序，
逐条下发并在每帧后等待该帧的 `interval` 毫秒。

> 执行模型（同步阻塞 vs 后台任务 + 进度查询）**待确认**：建议交由采集进程顺序执行，
> 主进程仅入队整个序列并返回任务 ID，前端轮询进度。

### 新增/修改入参

```json
{
  "seqId": null,
  "seqName": "上电初始化序列",
  "commands": [
    { "name": "K1502 驱动使能", "hex": "0A 91 00 04 00 04 AA AA", "interval": 2000 },
    { "name": "K1504 存储使能", "hex": "0A 92 00 0A 00 0A AA AA", "interval": 2000 }
  ],
  "remark": ""
}
```

> 序列内容应**排除广播帧**：前端在「从遥控指令添加」时即过滤；后端在保存时再次校验（见 07 章广播帧识别）。

---

## 6. 相机 `/payload/camera`

| 方法 | 路径                       | 说明                                    | 权限                  |
| ---- | -------------------------- | --------------------------------------- | --------------------- |
| GET  | `/payload/camera/telecontrol/config` | 相机遥控配置（`protocol=v16\|v17`） | `payload:camera:view` |
| GET  | `/payload/camera/telemetry/config`   | 相机遥测配置（`protocol=v16\|v17`） | `payload:camera:view` |
| POST | `/payload/camera/telecontrol/assemble` | 组帧（body 含 `protocol`）          | `payload:camera:view` |
| POST | `/payload/camera/telecontrol/send`   | 组帧并下发到 `deviceId`             | `payload:camera:view` |
| POST | `/payload/camera/start`    | 启动采集（指定串口、分辨率、图像序号）  | `payload:camera:view` |
| POST | `/payload/camera/stop`     | 停止采集                                | `payload:camera:view` |
| GET  | `/payload/camera/image`    | 最新图像 + 采集状态（Redis）            | `payload:camera:view` |
| GET  | `/payload/camera/status`   | 采集状态                                | `payload:camera:view` |

`protocol` 查询参数或 body 字段：`v16`（默认）使用 `xl-camera-tc` / `D8|D9`；`v17` 使用 `xl-camera-v17-tc` / `D8V17|D9V17`。

`POST /payload/camera/start`

```json
{ "port": "COM3", "resolution": "256×256", "imageNo": 1 }
```

v1.7 分辨率可为任意 `N×N`（N 为 8 的倍数，8–400），如 `"80×80"`。

`GET /payload/camera/image` → `{ image: { meta, data, format }, status: { deviceId, connected, message, state } }`，均读 Redis。  
图像由串口采集进程按 `EB 90` D6 协议重组后写入（见 [07 章](./07-遥控帧组装与遥测解析规则.md)）。

### 6.1 模拟页黄金样本 `/payload/telemetry/dev/sample(s)`

| 方法 | 路径 | 说明 |
| ---- | ---- | ---- |
| GET | `/payload/telemetry/dev/samples` | 按 `assemblerId`+`parserId` 返回可选样本列表 `{ key, label, tooltip }` |
| GET | `/payload/telemetry/dev/sample` | 按 `key` 或组装器+解析器返回单条 HEX 样本（不含 `fields`） |

权限：`payload:devtest:view`。实现：`tm_golden_samples.py` + `assets/data/tm_golden_cases.json`。

---

## 7. 配置文件 `/payload/config-files`

| 方法 | 路径 | 说明 | 权限 |
| ---- | ---- | ---- | ---- |
| GET | `/payload/config-files/list` | 可编辑 JSON 列表 | `payload:configfile:view` |
| GET | `/payload/config-files/content?name=` | 读取原文 | `payload:configfile:view` |
| PUT | `/payload/config-files/content` | 保存并触发热重载 | `payload:configfile:edit` |
| GET | `/payload/config-files/download?name=` | 下载 | `payload:configfile:view` |

---

## 8. 初始化任务（菜单与配置）

**当前做法**：业务菜单已固化在三套 `ruoyi-fastapi-*.sql` 中（见 [03](./03-数据库设计.md) §2）；  
遥测表类型不再每表一菜单，而由 `GET /payload/telemetry/pages` 运行时聚合各 `*-TeleMetryCfg.json`。

可选幂等脚本：读取配置 `page[]` → 补写 `sys_menu`（仅在对旧库升级且无法整库重建时使用）。

---

## 9. VO/出入参规范

- 入参 Pydantic 模型字段 snake_case，JSON 走 camelCase（`alias_generator=to_camel`）。
- 列表返回 `{ rows, total }`；详情返回 `{ data }`；操作返回 `CrudResponseModel`。
- 设备/遥测类接口多为「读 Redis / 写 Redis 队列」，不直接访问硬件。
